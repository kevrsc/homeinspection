/**
 * Pixel breakpoints per UX Design Specification → Breakpoint Strategy
 * (`_bmad-output/planning-artifacts/ux-design-specification.md`).
 *
 * Must stay aligned with `@theme` in `src/index.css` and Tailwind responsive prefixes.
 */
export const BREAKPOINTS_PX = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
} as const;
